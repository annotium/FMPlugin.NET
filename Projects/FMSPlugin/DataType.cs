using System;

namespace FMTechnologies
{
    /// <summary>
    /// Definitions for Plugin's Data Types 
    /// </summary>
    public enum DataType
    {
        DTInvalid,
        DTText,
        DTNumber,
        DTDate,
        DTTime,
        DTTimeStamp,
        DTBinary,
        DTBoolean
    };
}